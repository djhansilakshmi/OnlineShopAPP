@model OnlineShop_Web.Models.VM.AttributesDeleteVM

<form asp-action="DeleteAttributes">
    <br />
    <div class="container border p-3">
        <h1 class="text-primary">Delete Attributes</h1>
        <hr />
        <input hidden asp-for="attribute.AttributeId" />
        <input hidden asp-for="attribute.ProductID" value="@TempData["ProductId"]" id="hdnProductId" />

        <div class="row">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            @* Name  *@
            <div class="col-2">
                <label asp-for="attribute.Name" class="control-label pt-2" style="font-size:20px;"></label>
            </div>
            <div class="col-10 pb-3">
                <input asp-for="attribute.Name" disabled class="form-control" />
            </div>

            @* value  *@
            <div class="col-2">
                <label asp-for="attribute.Value" class="control-label pt-2" style="font-size:20px;"></label>
            </div>
            <div class="col-10 pb-3">
                <input asp-for="attribute.Value" disabled class="form-control" />
            </div>
            <div class="col-5 offset-2">
                <a asp-action="IndexAttributes" asp-controller="Attributes" asp-route-productId="@TempData["ProductId"]" class="btn-primary btn form-control ">Back to List</a>
            </div>
            <div class="col-5">
                <input type="submit" value="Delete" class="btn btn-success form-control" />
            </div>
        </div>
    </div>
</form>


@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
